<h1>Welcome <%= current_user.email if current_user %></h1>
<% if(signed_in?) %>
  <%= button_to "View Twitter Users", twitter_users_url, method: 'get',class: 'btn btn-default' %><br><br>
<% end %>

<%= button_to 'Sign Up', new_user_url, method: 'get', class: 'btn btn-default'%><br>
<%= button_to 'Log In', new_session_url, method: 'get', class: 'btn btn-default'%>
